% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_pretrends.R
\name{test_pretrends}
\alias{test_pretrends}
\title{Test for Pre-Trends via Event-Study Regression}
\usage{
test_pretrends(data, unit, time, outcome, treat, n_lags = 3)
}
\arguments{
\item{data}{A data frame containing balanced panel data.}

\item{unit}{Column name (unquoted) identifying the unit.}

\item{time}{Column name (unquoted) identifying the time period.}

\item{outcome}{Column name (unquoted) for the outcome variable.}

\item{treat}{Column name (unquoted) for the treatment indicator (0/1).}

\item{n_lags}{Integer. Number of leads (pre-treatment) and lags
(post-treatment) to include. Default is 3.}
}
\value{
An S3 object of class \code{"pretrends_test"} containing:
\describe{
\item{coefficients}{A \link[tibble:tibble]{tibble} with columns \code{term},
\code{estimate}, \code{std_error}, \code{statistic}, \code{p_value}, and \code{rel_time}.}
\item{f_test}{A list with elements \code{statistic} and \code{p_value} from the
joint Wald test of all pre-treatment lead coefficients.}
\item{model}{The fitted \link[fixest:feols]{fixest::feols} model object.}
}
}
\description{
Runs an event-study regression with unit and time fixed effects using
\code{\link[fixest:feols]{fixest::feols()}}. Returns a tidy table of pre-treatment lead coefficients
(with SEs and p-values) and a joint F-test for all pre-treatment
coefficients being zero. An \link[=autoplot.pretrends_test]{autoplot} method
produces the classic event-study coefficient plot.
}
\details{
The reference period is one period before treatment (relative time = -1),
which is omitted from the regression. Lead coefficients capture
pre-treatment dynamics: if the parallel trends assumption holds, these
should be close to zero.
}
\examples{
pt <- test_pretrends(staggered_data, unit, time, outcome, treat, n_lags = 3)
pt$coefficients
pt$f_test

}
